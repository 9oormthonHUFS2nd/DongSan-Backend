dependencies {
    implementation project(":infrastructure:storage-rdb") // 수정 예정
    testImplementation(testFixtures(project(":infrastructure:storage-rdb")))  // 수정 예정

    // validation
    implementation 'org.springframework.boot:spring-boot-starter-validation'  // 삭제 예정
}
